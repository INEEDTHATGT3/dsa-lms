{"level":3,"title":"Complexity Analysis + STL","tagline":"container internals, adversarial hashing, and the idioms MAANG expects","placementQuiz":[{"q":"std::map's tree is…","options":["AVL","red-black","B-tree","treap"],"answer":1,"explain":"Red-black: relaxed balance → fewer rotations, predictable log n."},{"q":"priority_queue lacks decrease-key. Dijkstra-style code compensates via…","options":["linear search","lazy deletion — push duplicates, skip stale pops","vector only","sorting each round"],"answer":1,"explain":"(node,betterDist) entries pushed freely; visited-check discards outdated ones."},{"q":"nth_element guarantees…","options":["full sort","the n-th position EXACT + partitioned around it, avg O(n)","heap only","stable order"],"answer":1,"explain":"Introselect: everything left ≤ pivot ≤ everything right, nothing more promised."},{"q":"Adversary feeds keys colliding in one unordered_map bucket. Defense?","options":["hope","custom randomized hash (splitmix64) per-run","switch to vector","bigger ints"],"answer":1,"explain":"Randomized seeding breaks precomputed collision attacks."},{"q":"emplace_back over push_back(large_obj) saves…","options":["nothing","the move/copy of a fully-constructed temporary — constructs in place","memory only","compile time"],"answer":1,"explain":"Args forwarded to constructor at the insertion site."}],"sections":[{"title":"Mental model — every container is a contract + a memory layout","blocks":[{"type":"prose","md":"MAANG probing goes one level under the API: *why* is it that complexity? The answers are always one of three stories: **contiguous memory** (cache-friendly scans), **pointer-chasing nodes** (stable addresses), or **hash buckets** (amortized bets). Map every STL container onto these three and you can derive its table instead of memorizing it."},{"type":"compare","leftTitle":"Layout","rightTitle":"Consequences","rows":[{"label":"Contiguous (vector/string/array)","left":"O(1) index · prefetch heaven · binary-searchable · reallocation moves everything"},{"label":"Nodes (list/map/set)","left":"stable iterators forever · pointer chase = cache misses · no indexing"},{"label":"Buckets (unordered_*)","left":"avg O(1) probes · worst O(n) chains · rehash storms"}]},{"type":"mantra","md":"**Choose by access pattern, not by habit: index-scan ⇒ vector · ordered queries ⇒ tree · membership storm ⇒ hash (with reserve).**"},{"type":"quiz","items":[{"q":"Why does map survive rehash-free while unordered_map doesn't?","options":["luck","nodes never move once allocated — only links change","smaller","tree sorts"],"answer":1,"explain":"Pointer structures mutate LINKS; element addresses persist → stable iterators."},{"q":"Binary search works on std::list?","options":["yes","no — needs O(1) random access; list mid-step is O(n)","with sort first","via iterator+"],"answer":1,"explain":"advance(mid) walks nodes — the whole win evaporates."}]}]},{"title":"Tree vs hash — the full trade-off script","blocks":[{"type":"followup","chain":[{"q":"When would you REJECT unordered_map despite average O(1)?","a":"Three honest cases: (1) need ORDERED iteration/range queries/lower_bound; (2) worst-case latency contracts — hash's O(n) tail breaks SLAs; (3) adversarial input environments without randomized hashing."},{"q":"What exactly does red-black guarantee vs AVL?","a":"RB: height ≤ 2·log(n+1), insert/delete ≤ 3 rotations amortized-cheap — good write-heavy default. AVL: stricter height (≈1.44·log n), faster lookups, costlier rebalances. std::map picked RB for balanced all-round behavior."},{"q":"How do you make hashing robust against hostile inputs?","a":"Per-process random seed mixed into std::hash (splitmix64 avalanche): identical inputs collide differently each run → attacker cannot precompute the kill-bucket. Show the struct-hash idiom on demand."},{"q":"unordered_map iteration cost anomaly?","a":"Iteration walks bucket-array THEN chains → Θ(buckets + n). After huge reserve with few elements, iterating is dominated by empty buckets — know this before micro-benchmarks."}]},{"type":"code","label":"splitmix64 custom hash (memorize shape)","code":"struct SplitMixHash {\n    static uint64_t splitmix64(uint64_t x) {\n        x += 0x9e3779b97f4a7c15ULL;                 // golden ratio\n        x = (x ^ (x >> 30)) * 0xbf58476d1ce4e5b9ULL;\n        x = (x ^ (x >> 27)) * 0x94d049bb133111ebULL;\n        return x ^ (x >> 31);\n    }\n    size_t operator()(uint64_t v) const {\n        static const uint64_t SEED =\n            chrono::steady_clock::now().time_since_epoch().count();\n        return splitmix64(v + SEED);\n    }\n};\nunordered_map<ll, int, SplitMixHash> safe;","py":"# PYTHON HASH SAFETY: str/bytes hashes are randomized per process\n# (PYTHONHASHSEED), so adversarial collision bombs mostly fail already.\n# Custom key classes still deserve thought:\nclass SplitMix:\n    def __init__(self): self.seed = id(object())   # per-run variation\n    def __call__(self, v):\n        x = (v + 0x9e3779b97f4a7c15) & 0xFFFFFFFFFFFFFFFF\n        x ^= x >> 30; x = x * 0xbf58476d1ce4e5b9 & 0xFFFFFFFFFFFFFFFF\n        x ^= x >> 27; x = x * 0x94d049bb133111eb & 0xFFFFFFFFFFFFFFFF\n        return x ^ (x >> 31)\n# dict with such keys needs explicit wrapper class __hash__/__eq__.","reveal":true},{"type":"quiz","items":[{"q":"Golden-ratio constant in hash mixing serves to…","options":["look cool","spread sequential keys across bit-space before avalanche steps","compress data","sign bits"],"answer":1,"explain":"Multiplicative hashing: consecutive integers land far apart pre-mixing."}]}]},{"title":"Heap mastery — the lazy-deletion doctrine","blocks":[{"type":"prose","md":"std::priority_queue exposes push/pop/top only. No decrease-key, no arbitrary erase. Every advanced algorithm (Dijkstra, two-heaps median, task schedulers) survives this via **lazy deletion**: obsolete entries stay inside; validity is checked AT POP time."},{"type":"trace","title":"Trace: sliding-window maximum via lazy max-heap (window=3, a=[4,2,7,1])","columns":["Step","pq contents (val,idx)","Pop phase","Valid top?"],"steps":[["push (4,0),(2,1)","{(4,0),(2,1)}","—","top (4,0) idx≥0 ✓"],["i=2 push (7,2)","{(7,2),(4,0),(2,1)}","top=(7,2)","✓ answer 7"],["i=3 push (1,3); window start=1","{(7,2),(4,0),(2,1),(1,3)}","top=(7,2) idx 2 ≥ 1","✓ answer 7"],["stale case","suppose window moved past idx2","pop (7,2) → DISCARD silently","next top judged"]]},{"type":"code","label":"the canonical guard","code":"priority_queue<pair<int,int>> pq;   // value, index\n// ... pushes happen freely ...\nwhile (!pq.empty() && -pq.top().second < left)   // stale?\n    pq.pop();                                    // discard, no ceremony\nint best = pq.top().first;","py":"import heapq\npq = []                            # lazy-deletion heap (value, index)\n# ... pushes happen freely ...\nwhile pq and stale(pq[0]):\n    heapq.heappop(pq)              # discard silently\nbest = pq[0]"},{"type":"callout","tone":"pink","md":"**Cost accounting interviewers love:** lazy deletion may hold O(total pushes) entries, but EVERY entry is pushed once and popped once ⇒ amortized O(log total) per op. State it as amortized analysis, not luck."},{"type":"heading","text":"Selection without sorting — nth_element"},{"type":"prose","md":"Need ONLY the k-th smallest (median, cutoff, percentile)? Full sort pays Θ(n log n) for order you won't read. `nth_element` introselects in **average Θ(n)**: exact element at position n-th, left side all ≤, right side all ≥ — nothing else promised."},{"type":"trace","title":"Trace: nth_element(v.begin()+4) on {9,1,8,2,7,3,6} (kth=idx4)","columns":["Phase","Array state","Invariant"],"steps":[["pivot chosen","introselect picks pivot","—"],["partition","{2,1,3, | 6 | ,9,8,7}","everything <6 sits LEFT of idx4"],["recurse one side","only side containing idx4 continues","quickselect discipline"],["end","v[4]==6 exact; sides unsorted-but-partitioned","avg Θ(n) achieved"]]},{"type":"quiz","items":[{"q":"After nth_element(k-iter), elements left of k are…","options":["sorted","all ≤ *k but arbitrarily ordered","all unique","reversed"],"answer":1,"explain":"Partition guarantee only — say 'unsorted but correctly divided' aloud."},{"q":"Two-heap running median uses lazy deletion because…","options":["style","heaps can't erase arbitrary middle elements — staleness filtered at top-pop","memory free","STL forces it"],"answer":1,"explain":"Same doctrine as Dijkstra; expect this sentence in system-y follow-ups."}]}]},{"title":"Production-grade vector discipline","blocks":[{"type":"list","items":["`reserve()` BEFORE bulk push loops — kills reallocation storms AND keeps pointers valid where you planned capacity","**erase-while-iterating**: use remove-erase (`v.erase(remove_if(...), end())`) or index rewinding — `v.erase(it)` inside range-for is UB territory after invalidation","`emplace_back(args...)` constructs in place — matters for string/struct members","`shrink_to_fit()` is a REQUEST, not a promise — don't build logic on capacity","swap-idiom `vector<T>().swap(v)` actually releases memory (destructor-time)"]},{"type":"code","label":"safe conditional erase (the idiom)","code":"// drop all even numbers, single pass, no iterator landmines\nv.erase(remove_if(v.begin(), v.end(),\n                  [](int x){ return x % 2 == 0; }),\n        v.end());\n\n// C++20 one-liner:\n// std::erase_if(v, [](int x){ return x % 2 == 0; });","py":"# remove-while-iterating list comprehension IS the idiom:\nv = [x for x in v if x % 2 != 0]   # drop evens, one clean pass\n# (no iterator invalidation exists - lists rebuild or mutate safely)"},{"type":"callout","tone":"blue","md":"**Cache story bonus point:** contiguous vectors beat node containers on real workloads even when big-O ties — misses cost ~100 cycles. Saying \"constant-factor via locality\" marks you senior."},{"type":"quiz","items":[{"q":"remove_if physically deletes elements?","options":["yes","no — compacts survivors forward, junk remains past returned iterator; erase trims","sorts them","allocates new"],"answer":1,"explain":"Two-phase design: reorder cheaply, then commit destruction once."}]}]},{"title":"Problem set — L3 hard (idioms under pressure)","blocks":[{"type":"problems","items":[{"id":"C301","tier":3,"title":"Longest Consecutive Sequence","platform":"LC","lc":128,"patterns":["hash-set-membership","sequence-start-detection"],"statement":"Longest run of consecutive integers in O(n) — sorting is the trap answer here.","hints":["Insert all into unordered_set.","Only START counting when x−1 absent — sequence heads only.","Each element visited ≤ twice total ⇒ O(n) defended."]},{"id":"C302","tier":3,"title":"Group Anagrams","platform":"LC","lc":49,"patterns":["canonical-key-design"],"statement":"Group words that are anagrams. Then defend your key: sorted-string vs count-vector.","hints":["Canonical form = sorted letters OR 26-count tuple.","Key must be EQUALITY-canonical, not human-readable.","Discuss hash-of-custom-struct requirements if avoiding string keys."]},{"id":"C303","tier":3,"title":"Top K Frequent Elements (O(n) variant)","platform":"LC","lc":347,"patterns":["bucket-sort-by-frequency","heap-alternative"],"statement":"Solve with heap (n log k), then upgrade to frequency buckets for O(n); explain why bucketing is legal here.","hints":["freq map → buckets indexed BY frequency (≤ n always).","Walk buckets from high to low collecting k.","Legality: frequencies bounded by n ⇒ linear space."]},{"id":"C304","tier":3,"title":"Subarray Sum Equals K","platform":"LC","lc":560,"patterns":["prefix-sum","hash-count-pairs"],"statement":"Count subarrays summing to k in one pass. The hashmap stores PREFIX COUNTS, not positions.","hints":["need = prefix − k; add freq[need] before inserting current prefix.","Seed freq[0]=1 for prefixes starting at 0.","Explain why sliding window FAILS with negatives."]},{"id":"C305","tier":3,"title":"K Closest Points to Origin","platform":"LC","lc":973,"patterns":["nth_element-selection","heap-alternative"],"statement":"k closest points three ways: full sort, size-k heap, nth_element — compare costs and stability notes.","hints":["No sqrt needed — compare squared distances.","nth_element on distance comparator = average O(n).","Follow-up bait: streaming version? (heap wins)"]},{"id":"C306","tier":3,"title":"Design Twitter Timeline Feed","platform":"LC","lc":355,"patterns":["multi-map-state","merge-k-lists-preview"],"statement":"Post/follow/unfollow/getNewsFeed(10 latest). Compose user→tweets map + follower sets + k-way merge of recent lists.","hints":["Store (time,tweetId) per user, global clock counter.","Feed = merge candidate lists by recency — heap of list-heads.","Unfollow ≠ delete tweets (history persists)."]},{"id":"C307","tier":3,"title":"Insert Delete GetRandom O(1) with DUPLICATES allowed","platform":"variant","lc":381,"patterns":["swap-to-end-delete","index-vectors"],"statement":"Extend LC380: map value→VECTOR of indices; delete pops an index, swap-to-end patches it.","hints":["value→indices vector; lazily skip stale indices on getRandom? No — patch eagerly.","Delete idx: move last element INTO idx, fix its index entry, pop_back.","Edge: deleting the element you just moved — check identity."]}]}]}],"revisionCard":["Three layout stories: contiguous / node-chase / hash-buckets — derive costs from them","Reject hash when order needed, latency tails matter, or adversaries lack a randomized hash","Lazy deletion: push freely, validate at pop — amortized cost argument required","nth_element: exact k-th + partitioned sides at avg Θ(n) — selection ≠ sorting","reserve() before storms · remove-erase idiom · emplace_back for heavy members","Sequence-start detection trick makes LC128 linear — visit-count defense","Prefix-COUNT maps (freq[0]=1 seed) solve subarray-sum-k where windows fail (negatives)"],"glossary":[{"term":"red-black tree","def":"Relaxed-height BST backing map/set; rotation-cheap writes."},{"term":"avalanche hashing","def":"Bit-mixing (splitmix64) making outputs decorrelate from input patterns."},{"term":"lazy deletion","def":"Leaving invalidated heap entries, filtering them during pops; validity checked lazily."},{"term":"introselect","def":"Quickselect + fallback strategy behind nth_element; average-linear selection."},{"term":"remove-erase idiom","def":"remove_if compacts survivors; erase commits truncation — the safe filter pattern."},{"term":"canonical key","def":"Normalized representation (sorted chars, count-tuple) making equivalence classes collide."},{"term":"bucket sort by statistic","def":"When a value is provably ≤ n, index buckets by it for linear grouping."}],"moduleNum":"01"}